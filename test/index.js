/*global it: true, describe: true */
/*eslint no-console: 0*/

import should from 'should'
import lib from '../src'

describe('sutro-client', () => {
  it('should exist', async () => {
    should.exist(lib)
  })
})
